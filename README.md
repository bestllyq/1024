1024 Game

这是一个简单的 1024 游戏静态网站。

运行方法：

1. 直接在浏览器中打开 `index.html`。

2. 或者在项目目录运行一个静态服务器，例如：

```bash
# Python 3
python -m http.server 8000

# 或者使用 http-server（需要 npm）
npx http-server -c-1 . 8000
```

在浏览器中访问 `http://localhost:8000` 即可开始游戏。祝你玩得开心！
